/** Re-exports useTheme from the ThemeProvider — matches admin panel hook pattern. */
export { useTheme } from '@/components/providers/ThemeProvider';
export type { UseThemeReturn } from '@/components/providers/ThemeProvider';
