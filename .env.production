# Vercel production — use proxy path so browser never calls HTTP from HTTPS
# The real backend URL lives only in vercel.json (server-side, not in the JS bundle)
VITE_APP_BACKEND_API_BASE_URL="/backend-api"
